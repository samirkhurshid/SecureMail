# Email Security Gateway — Python dependencies
# Compatible with modern Python environments (including Python 3.14+)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP client (async)
httpx>=0.27.0

# Pydantic v2 + settings
pydantic>=2.9.0
pydantic-settings>=2.4.0

# Email parsing
mail-parser>=3.15.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

# Dev / optional
python-multipart>=0.0.9   # for file upload endpoints
python-dotenv>=1.0.0      # configuration loading
